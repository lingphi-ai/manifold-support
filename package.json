{
  "name": "manifold-support",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "build": "node scripts/build-site.mjs",
    "test": "node scripts/verify-support-docs.mjs",
    "verify": "npm run build && npm test"
  }
}
